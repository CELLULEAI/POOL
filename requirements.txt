# Dependances runtime du pool — MIROIR de pyproject.toml (source de verite).
# Sert le Dockerfile racine + fly.io/render (chemin simplifie). La prod federee
# passe par le wheel signe (docker/Dockerfile), PAS par ce fichier.
# Pour le smart-routing KNN, installer l'extra : pip install "iamine-ai[pool]".
llama-cpp-python>=0.2.0
fastapi>=0.110.0
uvicorn>=0.27.0
websockets>=12.0
psutil>=5.9.0
aiohttp>=3.9.0
huggingface-hub>=1.0.0,<2
cryptography>=41.0.0
passlib>=1.7.0
argon2-cffi>=21.0.0
asyncpg>=0.29.0
